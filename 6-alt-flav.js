// an alternate to do the same thing
// objects too like array and JS objects can be exported

module.exports.items = ['item1', 'item2']

const person = { name: 'Sharad' }

module.exports.oneperson = person
