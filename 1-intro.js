const { captureRejectionSymbol } = require('events')

// console.log('Hello world')
const amount = 12
if (amount < 9) {
  console.log('it is a samll amount')
} else {
  console.log('it is a big amount')
}
console.log(` multi line back tog quotes !!!!!`)
